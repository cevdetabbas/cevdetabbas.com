version: '3.8'
services:
  web:
    image: httpd:latest
    container_name: cevdetabbas.com
    ports:
      - "1880:80"
    volumes:
      - ./htdocs:/usr/local/apache2/htdocs/
    command: >
      bash -c "
        apt-get update &&
        #apt-get install -y git &&
      "
    restart: always
